# NapCat 状态检测测试脚本 (JavaScript)

通过 WebSocket 检测 NapCat 容器的在线状态。

## 快速开始

```bash
# 进入 js 目录
cd js

# 安装依赖
npm install

# 检测 Bot 在线状态
npm run test:status

# 获取 Bot 登录信息
npm run test:login
```

## 脚本说明

| 命令 | 说明 |
|------|------|
| `npm run test:status` | 调用 `get_status` 接口，检测 Bot 是否在线 |
| `npm run test:login` | 调用 `get_login_info` 接口，获取 Bot 昵称和 QQ 号 |

## 配置

默认连接地址在脚本中硬编码：
- 地址: `ws://192.168.31.233:3000`
- Token: `test`

如需修改，编辑 `test/test_get_status.js` 中的 `uri` 变量。

## 示例输出

```
✅ 已建立 WebSocket 连接: ws://192.168.31.233:3000?access_token=test
🚀 正在请求接口: get_status...

========== 状态检测结果 ==========
在线状态 (online): 🟢 在线
运行正常 (good):   ✅ 是

✨ Bot 运行完美！
```
